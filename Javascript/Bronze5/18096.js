// const input = require("fs").readFileSync("/dev/stdin").toString();
const input = "3".toString();
function Арифметическаямагия(input) {
    const N = Number(input);
    console.log(1);
}

Арифметическаямагия(input);