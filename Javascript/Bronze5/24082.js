// const input  = require("fs").readFileSync("/dev/stdin").toString().trim();
const input = "4".toString().toString().trim();

function 立方体(input) {
    return Number(input) ** 3;
}

console.log(立方体(input));