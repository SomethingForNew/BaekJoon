// const input  = require("fs").readFileSync("/dev/stdin").toString().trim();
const input = "2".toString().trim();

function sciComLove(input) {
    for(let i = 0; i < input; i++) {
        console.log("SciComLove");
    }
}

sciComLove(input);