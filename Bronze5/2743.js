// const input  = require("fs").readFileSync("/dev/stdin").toString().split("\n");
const input = "pulljima".toString().trim();

function checkTheLength(input) {
    console.log(input.length);
}

checkTheLength(input);