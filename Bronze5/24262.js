// const input  = require("fs").readFileSync("/dev/stdin").toString().trim();
const input  = "1".toString().trim();

function algorithmClass(input) {
    console.log(1);
    console.log(0);
}

algorithmClass(input);